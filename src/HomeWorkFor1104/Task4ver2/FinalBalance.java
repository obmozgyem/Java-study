package HomeWorkFor1104.Task4ver2;

import java.io.*;
import java.util.ArrayList;
import java.util.List;
import java.util.HashMap;
import java.util.Map;
/*
public class FinalBalance
{
    public static void main(String[] args) {
        for (inti=0; i< trList.size()) {
            if (!balanceMap.containsKey(trList[i].getName()) || !balanceMap.containsKey(trList[i].getToWho())) {
                continue;
            }
            else {
                int minus = balanceMap.get(trList[i].getName());
                if (minus < trList[i].getSum()){
                    continue;
                }
                else {
                    int finall = minus - trList[i].getSum();
                    balanceMap.put (trList[i].getName(), finall);
                    int pluse = balanceMap.get(trList[i].getToWho());
                    int finall2 = pluse + trList[i].getSum();
                    balanceMap.put (trList[i].getToWho(), finall2);
                }
            }
        }
    }
}
*/
